-- ==========================================================
-- เพิ่มตาราง site_visits — เก็บยอดเข้าชมเว็บรวมของแต่ละวัน (1 แถวต่อวัน นับรวมทุกคน ไม่แยกบัญชี)
-- ใช้แสดงในแบนเนอร์ "วันนี้" บนหน้าหลัก (TodaySummary.jsx) แทนที่สถิติ "เช็คชื่อวันนี้" เดิม
-- ==========================================================

CREATE TABLE IF NOT EXISTS site_visits (
  date  DATE PRIMARY KEY,
  count INT NOT NULL DEFAULT 0
);
