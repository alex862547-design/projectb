-- ==========================================================
-- เพิ่มคอลัมน์ edited_at ให้ตาราง checkins — บันทึกว่าถูก "แก้ไข" ทีหลังตอนไหน (เช่น แอดมินแก้สถานะ/เวลา/
-- วันที่ผ่านหน้า "จัดการเช็คชื่อ") แยกจากตอนเช็คชื่อครั้งแรก (สร้างแถวใหม่ไม่ถือว่าแก้ไข, edited_at เป็น NULL)
-- ใช้โชว์ป้าย "มีการเปลี่ยนแปลง" ในหน้า "ประวัติของฉัน" (UserHistory.jsx)
-- ==========================================================

ALTER TABLE checkins ADD COLUMN IF NOT EXISTS edited_at TIMESTAMP;
