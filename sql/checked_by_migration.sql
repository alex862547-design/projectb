-- บันทึกว่า "ใครเป็นคนกดเช็คชื่อ/เช็คขาด" ให้แถวเช็คชื่อแต่ละแถว เอาไว้โชว์ในหน้าต่างข้อความ (AttendanceThreadModal)
-- อ้างอิงไปที่ users.id เพราะครอบคลุมทั้งนักศึกษาที่มีสิทธิ์เช็คชื่อและแอดมิน (ใช้ระบบล็อกอินเดียวกัน)
-- แถวเช็คชื่อเก่าก่อนไฟล์นี้จะเป็น NULL (ไม่ทราบผู้เช็คชื่อ) เพราะไม่เคยมีการบันทึกไว้ ไม่กระทบข้อมูลเดิมที่มี
ALTER TABLE checkins ADD COLUMN checked_by_id INT REFERENCES users(id) ON DELETE SET NULL;
